default: &default
  dispatchers:
    - polling_interval: 100
      batch_size: 500
  workers:
    - queues: "*"
      threads: 10
      processes: 1
      polling_interval: 100

development:
 <<: *default

test:
 <<: *default

production:
 <<: *default
