<%= turbo_frame_tag "reminders_list" do %>
  <% if reminders.empty? %>
    <%= render partial: 'reminders/no_reminders'%>
  <% else %>
    <ul class="list-group list-group-flush">
      <%= render partial:'reminders/reminder', collection: reminders %>
    </ul>
  <% end %>
<% end%>